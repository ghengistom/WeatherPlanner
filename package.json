{
  "name": "nodebootstraper",
  "version": "5.0.0",
  "dependencies": {
    "axios": "^1.6.8",
    "body-parser": "^1.20.2",
    "cors": "^2.8.5",
    "express": "*",
    "method-override": "*",
    "morgan": "*",
    "pug": "*",
    "serve-favicon": "*",
    "sqlite3": "^5.1.7",
    "stylus": "*"
  },
  "engines": {
    "node": "*",
    "npm": "*"
  },
  "proxy": "http://localhost:0.0.0.0",
  "scripts": {
    "start": "node app.js",
    "heroku-postbuild": "cd client && npm install --only=dev && npm install && npm run build"
  }
}
